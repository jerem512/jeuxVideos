{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Retrouvez tous nos jeux vidéos</h1>
    <div class="container">
        <div class="row flex">
            {%  for article in articles %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <div class="card-title text-center">
                                <figure>
                                    <img src="{{ asset('assets/media/jeux.jpg') }}" alt="">
                                    <figcaption>
                                        <a href="#">{{ article.name }}</a>
                                        <p>{{ article.descritpion }}</p>
                                        <p>{{ article.note }}</p>
                                    </figcaption>
                                </figure>

                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
