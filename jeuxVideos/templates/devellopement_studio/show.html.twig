{% extends 'base.html.twig' %}

{% block title %}DevellopementStudio{% endblock %}

{% block body %}
    <h1>DevellopementStudio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devellopement_studio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ devellopement_studio.name }}</td>
            </tr>
            <tr>
                <th>DateOfCreation</th>
                <td>{{ devellopement_studio.dateOfCreation ? devellopement_studio.dateOfCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>WebSiteLink</th>
                <td>{{ devellopement_studio.webSiteLink }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('devellopement_studio_index') }}">back to list</a>

    <a href="{{ path('devellopement_studio_edit', {'id': devellopement_studio.id}) }}">edit</a>

    {{ include('devellopement_studio/_delete_form.html.twig') }}
{% endblock %}
